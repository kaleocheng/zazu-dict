# zazu-dict
Zazu plugin to query word with the built‐in dictionary on  Mac


## Install

add this plugin in `~/.zazurc.json`:

```json
plugins": [
      "kaleocheng/zazu-dict"
  ]
```

and then reload config of zazu

## Usage

the default key word is `dc`:

![example](example.png)


